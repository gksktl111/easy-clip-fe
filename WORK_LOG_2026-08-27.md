# 2026-08-27 작업 기록

## 커밋 기준 완료 내역

- `fbc8747` — `#141 / refactor(sidebar) : 워크스페이스 라우트와 사이드바를 분리`
  - 보호 워크스페이스 라우트·레이아웃과 사이드바 구성 요소를 분리했다.
  - 폴더 경로 처리와 워크스페이스 사이드바 구조를 정리했다.

- `40e0a27` — `#141 / fix(folder) : 폴더 사이드바 실패 상태와 재시도를 안내`
  - 폴더 조회의 로딩·빈 목록·오류 상태를 구분하고, 초기 조회 실패 시 재시도 버튼을 제공했다.
  - 폴더 생성·이름 변경·삭제·순서 변경 실패에 오류 토스트를 추가했다.
  - mutation 진행 중 중복 실행을 막고, 이름 입력 모달의 확인 버튼을 비활성화했다.
  - 4개 로케일에 폴더 오류·재시도 문구를 추가했다.

- `64b5b4f` — `#141 / refactor(auth) : 인증 상태와 보호 경로 구조를 정리`
  - `SessionProvider`·`useSession`을 `AuthProvider`·`useAuth`로 변경했다.
  - `AuthGuard`를 `features/auth/ui`로 이동하고, 보호 레이아웃은 이를 조합만 하도록 정리했다.
  - 전역 인증 상태 계약은 `shared/auth`에 두어 다른 feature의 직접 의존을 피했다.
  - OAuth 공급자 타입은 컴포넌트 이름과 구분되도록 `OAuthProvider`로 변경했다.
  - 인증 정보 확인 실패 화면에 재시도와 홈 이동 버튼 및 4개 로케일 문구를 추가했다.

## 검증

- `npm run lint` 통과
- `npm test` 통과 — 8개 테스트 파일, 25개 테스트
- `npm run build` 통과
- `git diff --check` 통과

## 다음 작업

1. `AuthProvider` 책임 축소
   - `useRouter`, `router.replace`, `router.refresh`, 사용자 토스트를 Provider에서 제거한다.
   - Provider는 세션 복구·로그아웃 API·캐시 정리·`AuthStatus` 전이만 담당한다.
   - `AuthGuard`가 미인증 로그인 이동과 인증 실패 UI를 단독으로 담당한다.

2. 인증 실패 화면의 홈 이동 복구 흐름 확정
   - 현재 인증 쿠키가 있으면 Proxy가 `/`를 `/favorites`로 보낸다.
   - 액세스 토큰을 무조건 지우기보다, 오류 화면에서만 홈 접근을 한 번 허용하는 recovery 흐름과 Proxy 정책을 설계·검증한다.

3. 인증 흐름 테스트 보강
   - `AuthProvider`의 `401`·`404`·네트워크 오류·로그아웃 상태 전이를 테스트한다.
   - `AuthGuard`의 로딩·로그인 이동·재시도 화면을 테스트한다.

4. 폴더 실패 UX 수동 확인
   - 조회 실패·재시도·mutation 실패·중복 클릭 방지를 브라우저에서 확인한다.
